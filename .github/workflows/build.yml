name: Build CAP Relay APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.27.0'
          channel: 'stable'
      
      - name: Build APK
        run: flutter build apk --release || true
        env:
          FLUTTER_ALREADY_WATCHING: 1
      
      - name: Find any APK
        run: |
          find /home/runner -name "*.apk" -type f 2>/dev/null | head -5
          find . -name "*.apk" -type f 2>/dev/null | head -5
      
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: apk
          path: "**/*.apk"
